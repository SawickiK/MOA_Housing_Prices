# Project Structure

`data` folder - data for traing testing and generated submission for kaggle

`data_preparation.ipynb` - notebook used for preprocessing, output of notebook is `df_train` and `df_test`

`model_training.ipynb` - notebook for training various models

`final_model.ipynb` - notebook for generating prediction for kaggle 
